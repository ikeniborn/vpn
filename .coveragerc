[run]
source = vpn
branch = True
omit = 
    tests/*
    vpn/cli/main.py
    */migrations/*
    */venv/*
    */.venv/*
    */site-packages/*
    */tests/*
    setup.py
    venv/*
    .venv/*

[report]
precision = 2
show_missing = True
skip_covered = False
exclude_lines =
    pragma: no cover
    def __repr__
    if self.debug:
    if settings.DEBUG
    raise AssertionError
    raise NotImplementedError
    if 0:
    if __name__ == .__main__.:
    class .*\bProtocol\):
    @(abc\.)?abstractmethod
    # Type checking imports
    if TYPE_CHECKING:
    # Platform specific
    if sys.platform
    # Defensive assertions
    assert False
    # Fallback code
    except ImportError:

[html]
directory = htmlcov
title = VPN Manager Coverage Report

[xml]
output = coverage.xml